import { style } from '@vanilla-extract/css';

export const entry = style({
  position: 'relative',
  backgroundColor: '#fff',
  userSelect: 'none',
  cursor: 'default',
  outline: 'none',
  overflow: 'hidden',
  flexGrow: 1,
});
