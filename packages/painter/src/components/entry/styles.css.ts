import { style } from '@vanilla-extract/css';

export const entry = style({
  position: 'relative',
  width: '100%',
  height: '100%',
  backgroundColor: '#fff',
  userSelect: 'none',
  cursor: 'default',
  outline: 'none',
  overflow: 'hidden',
});
