import React from 'react';
import { usePainterHook, usePainterService } from '../../../../context';
import { IDrawLayerProps } from '../../../../types';
import { createSorter } from '../../../../utils';

export function Render({ parts, lines }: IDrawLayerProps) {
  return (
    <>
    </>
  );
}
