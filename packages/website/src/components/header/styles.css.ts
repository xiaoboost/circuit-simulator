import { Colors } from '@circuit/shared';
import { style } from '@vanilla-extract/css';

export const header = style({
  height: 44,
  width: '100%',
  backgroundColor: Colors.BorderLight.toString(),
});
